import { LoginForm } from "@/components/app/login/LoginForm";

const LoginPage = () => {
  return (
    <section className="w-full h-dvh flex justify-center items-center bg-dashboard-primary">
      <LoginForm />
    </section>
  );
};

export default LoginPage;
